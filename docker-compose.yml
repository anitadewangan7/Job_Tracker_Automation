version: "3.9"

services:

  postgres:

    image: postgres:16

    environment:
      POSTGRES_DB: job_tracker
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

    ports:
      - "5432:5432"

  app:

    build: .

    depends_on:
      - postgres